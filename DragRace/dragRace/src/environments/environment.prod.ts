import { Environment } from './environment.model';

export const ENV: Environment = {
  PRODUCTION: true,
  ENV_NAME: 'PRO',
  API: {
    URL: 'http://www.nokeynoshade.party/api'
    //VERSION: 'v1'
  }
};
